version: '3.9'

services:
  mongo:
    image: mongo:7.0
    container_name: vulnscan-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - vulnscan-net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: vulnscan-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/vulnscan
      - RATE_LIMIT_WINDOW_MS=900000
      - RATE_LIMIT_MAX=20
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - vulnscan-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: vulnscan-ui
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - vulnscan-net

networks:
  vulnscan-net:
    driver: bridge

volumes:
  mongo_data:
